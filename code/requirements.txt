# Core dependencies
#
# Note: Torch wheels are platform-specific. If you need a particular CUDA build,
# install torch following https://pytorch.org/get-started/locally/.
torch>=2.0

# RL4CO provides the benchmark/training stack targeted by this repo.
rl4co==0.6.0

# Testing
pytest>=8.0

# Dev tooling (recommended)
ruff>=0.5.0
mypy>=1.8.0
